
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CONFIGURATION: production # ou homolog
    container_name: advtasker-frontend
    ports:
      - "8080:80"
